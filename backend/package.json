{
  "name": "job-tracker-backend",
  "version": "1.0.0",
  "description": "Lambda backend for Job Application Tracker",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": ["aws", "lambda", "dynamodb"],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "aws-sdk": "^2.1400.0"
  },
  "devDependencies": {
    "eslint": "^8.40.0"
  }
}
